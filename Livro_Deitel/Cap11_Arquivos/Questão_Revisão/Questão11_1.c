/*
Preencha as lacunas de cada uma das sentenças a seguir:
a) Em última análise, todos os itens de dados processados por um computador são reduzidos
a combinações de _ e _.
b) O menor item de dado que um computador pode processar é chamado _.
c) Um _ é um grupo de registros relacionados entre si.
d) Dígitos, letras e símbolos especiais são chamados_.
e) Um grupo de arquivos relacionados entre si é chamado _.
f) A função _ fecha um arquivo.
g) A instrução _ lê dados de um arquivo, de forma similar ao modo como scanf lê de stdin.
h) A função _ lê um caractere de um arquivo especificado.
i) A função _ lê uma linha de um arquivo especificado.
j) A função _ abre um arquivo.
k) A função _ é usada normalmente na leitura de dados de um arquivo em aplicações de
acesso aleatório.
l) A função _ reposiciona o ponteiro de posição do arquivo em um local específico do arquivo.

Respostas
a) 0s e 1s 
b) bit
c) arquivo
d) caracteres
e) banco de dados
f) fclose()
g) fscanf()
h) fgetc()
i) fgets()
j) fopen()
k) fread()
l) fseek()

*/